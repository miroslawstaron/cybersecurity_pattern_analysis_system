{
    "code": "error = scanf(%d, &m);
    if ( EOF == error ){
        printf(No integer passed: Die evil hacker!\n);
        return 1; // Exit the program
    }",
    "model": "singberta",
    "vulnerability": "CVE_20",
    "type": "SCE"
}